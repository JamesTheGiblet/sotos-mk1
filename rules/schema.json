{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "condition": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["and", "or", "not", "price", "volume", "rsi", "ma", "volatility", "pattern", "time", "zscore"]
        },
        "params": { "type": "object" },
        "conditions": { "type": "array", "items": { "$ref": "#/definitions/condition" } }
      },
      "required": ["type"]
    }
  }
}
